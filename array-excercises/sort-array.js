let unsorted = [3, 2, 1];

let sorted = unsorted.sort((a, b) => a - b)

console.log(sorted);

let unsortedChars = ['a', 'b', 'c', 'e', 'd'];

let sortedChars = unsortedChars.sort();

console.log(sortedChars);